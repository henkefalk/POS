package se.kth.IV350.integration;

import se.kth.IV350.model.Receipt;

public class Printer {

	/**
	 * This is were the printer would print the receipt
	 * @param receipt the receipt from the sale
	 */
	public static void printReceipt(Receipt receipt) {

	}
}
